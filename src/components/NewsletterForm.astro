---
/**
 * Newsletter-Formular mit Formspree-Integration.
 * Ersetzen Sie die action-URL durch Ihre eigene Formspree- oder Beehiiv-Endpoint-URL.
 */
const {
  action = 'https://formspree.io/f/mzblllaq',
  headline = 'Bleiben Sie informiert',
  description = 'Erhalten Sie Einladungen zur Beta, Experten-Insights und Updates zum Launch der Ruhestarter Plattform.'
} = Astro.props;
---
<section id="newsletter" class="section items-center gap-10 rounded-[3rem] border border-petrol/10 bg-white/80 shadow-card backdrop-blur">
  <header class="section-header">
    <h2 class="section-title">{headline}</h2>
    <p class="section-subtitle">{description}</p>
  </header>
  <form
    class="flex w-full flex-col items-center gap-4 sm:flex-row sm:justify-center"
    action={action}
    method="POST"
  >
    <label class="sr-only" for="email">E-Mail-Adresse</label>
    <input
      class="w-full max-w-md rounded-full border border-petrol/30 bg-white px-6 py-3 text-base text-slate shadow-inner focus:border-ocean focus:outline-none focus:ring-2 focus:ring-ocean/40"
      id="email"
      name="email"
      type="email"
      placeholder="Ihre E-Mail-Adresse"
      required
    />
    <button class="btn-primary w-full max-w-[12rem]" type="submit">
      Jetzt vormerken
    </button>
  </form>
  <p class="text-center text-sm text-slate/70">
    Wir versprechen: keine Spam-Flut. Sie erhalten nur relevante Updates rund um Ruhestarter und den Ruhestands-Kompass.
  </p>
</section>
